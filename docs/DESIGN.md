# 코드 작성 컨벤션

<!-- Stack의 DESIGN.md는 Kotlin/Spring + React 웹 컨벤션이라 이관 대상에서 제외했다.
     앱 스택 확정 후 이 골격을 채운다. 원칙: 컨벤션은 "예시 코드"와 함께 기술해야
     에이전트가 흉내낼 수 있다 (규칙 나열만으로는 drift 발생 — Stack 운영 경험). -->

## 1. 공통 원칙
- 기존 코드 패턴 우선 — 새 패턴 도입은 이 문서 갱신과 함께만
- 계층 분리: UI에 비즈니스 로직 금지
- 실패는 조용히 삼키지 않는다 (에러 처리 규약 명시)

## 2. 명명 규칙

| 대상 | 규칙 | 예시 |
|------|------|------|
| 파일/클래스 | | |
| 화면(Route/Screen) | | |
| 서비스/매니저 | | |
| 모델/DTO | | |

## 3. 디렉토리/모듈 배치 규칙

## 4. 상태관리 패턴

## 5. 데이터 접근 패턴

## 6. 에러 처리·로깅 규약

## 7. 설정값 관리
<!-- Stack에서 이관할 핵심 규칙: 시크릿은 코드/설정파일에 평문 금지 — 환경변수·시크릿 스토어로만.
     비-시크릿(URL, 경로, 모드)은 평문 OK. 런타임 조정이 필요한 값은 설정 계층으로 분리. -->
