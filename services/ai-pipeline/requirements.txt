# 정확한 버전 고정 — CLAUDE.md §의존성 (lock 파일 없는 언어는 핀 고정)
# 갱신 절차: .venv에서 설치 후 pip freeze로 이 파일 재생성
celery[redis]==5.6.3
redis==6.4.0
python-dotenv==1.2.3
# vision-scoring
torch==2.13.0
open_clip_torch==3.3.0
pillow==12.3.0
opencv-python-headless==5.0.0.93
psycopg[binary]==3.3.5
# audio-beat
librosa==0.11.0
soundfile==0.14.0
